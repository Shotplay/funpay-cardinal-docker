{{- define "env" -}}
{{- if .env -}}
ENV \
{{- $first := true }}
{{- range $key, $value := .env }}
{{- if not $first }} \
{{- end }}
  {{ $key }}="{{ $value }}"
{{- $first = false }}
{{- end }}
{{- end -}}
{{- end -}}