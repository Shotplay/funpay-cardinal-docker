services:
  funpay-cardinal:
    container_name: funpay-cardinal
    image: shotplay/fp-cardinal:latest
    restart: unless-stopped
    stdin_open: true
    tty: true
    volumes:
      - ./configs:/cardinal/configs
      - ./plugins:/cardinal/plugins
      - ./storage:/cardinal/storage
